function solve(input) {
    for (let i = 0; i < input; i++) {
        let result = '';
        for (let i = 0; i < input; i++) {
            result += '* ';
        }
        console.log(result);
    }
}

solve(4);